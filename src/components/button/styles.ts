import styled from 'styled-components';

export const Button = styled.button`
    width: 100%;
    border-radius: 2rem;
    background: var(--red);
    color: var(--white);
    border: 0;
    padding: 19px 24px;
    font-weight: var(--bold);
`